build:
  box: samueldebruyn/debian-git
  steps:
    - script:
        name: sync
        code: |
          bash sync.bash
  after-steps:
    - wantedly/pretty-slack-notify:
          webhook_url: $SLACK_WEBHOOK_URL
